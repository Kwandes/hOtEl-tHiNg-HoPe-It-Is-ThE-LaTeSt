Assumptions (not discussed, to be discussed during the next meeting)
-Teo, why are your methods in GuestUI static?
-Let's think about your print methods (design the UI)

Facts:

Conventions:
camelCase vars, CamelCase classes
if()
{
	doSomething();
}

Interface = classManagement
user interaction = UI
file names: /files./users.txt
config: ./config.cfg

Other:
-Room price is adjustable globally via config and locally (per object) via a setter
-MF <> FileManagement communciation is handled using an Information object
-Passwords will be hashed

